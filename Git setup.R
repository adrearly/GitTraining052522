usethis::use_git_config(user.name = "adrearly", user.email = "adreanna.early@gmail.com")
credentials::set_github_pat() # You will be prompted to provide your GitHub PAT credentials::git_credential_forget() to forget credentials

# edited new comment 1 in GitHub
# added new line in RStudio
